name: Build Cutter (Qt6)

env:
  TAG: "v2.4.1"
  RIZIN_TAG: "v0.8.2"

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build-linux-arm64:
    name: linux-arm64
    runs-on: ubuntu-26.04-arm

    # Run the entire build inside Ubuntu 25.10 ARM64.
    container:
      image: ubuntu:25.10

    env:
      DEBIAN_FRONTEND: noninteractive

    steps:

    - name: Set timezone
      run: |
        export TZ=UTC
        ln -snf /usr/share/zoneinfo/$TZ /etc/localtime
        echo $TZ > /etc/timezone

    - name: Install bootstrap dependencies
      shell: bash
      run: |
        apt-get update
        apt-get install -y \
          git \
          ca-certificates

    - name: Clone cutter repository (stable branch)
      run: |
        git clone --depth 1 --recursive --branch ${{ env.TAG }} https://github.com/rizinorg/cutter.git .

        git config --global --add safe.directory "$(pwd)"
        git config --global --add safe.directory "$(pwd)/rizin"

        cd rizin
        git fetch --depth 1 origin tag ${{ env.RIZIN_TAG }}
        git checkout ${{ env.RIZIN_TAG }}

    - name: Fix git subprojects problems
      shell: bash
      run: |
        rm -rf build
        git submodule foreach --recursive git reset --hard
        git submodule foreach --recursive git clean -dxff

        cd rizin
        git clean -dxff subprojects/

    - name: Fix file options dialog height
      shell: bash
      run: |
        # We used 'diff -e basefile changedfile > patch.txt'
        # than we create 'sed' commands base on patch.txt
        sed -i \
          -e '10c\    <height>684</height>' \
          -e '14c\   <sizepolicy hsizetype="Minimum" vsizetype="Preferred">' \
          -e '22c\    <height>500</height>' \
          -e '119c\      <sizepolicy hsizetype="Minimum" vsizetype="Preferred">' \
          -e '421c\           <height>310</height>' \
          -e '473c\                <enum>QLayout::SetDefaultConstraint</enum>' \
          src/dialogs/InitialOptionsDialog.ui

    - name: Install build dependencies
      run: |
        apt-get update

        # Install basic tools
        apt-get install -y \
          build-essential \
          cmake \
          ninja-build \
          pkg-config \
          dpkg-dev \
          git \
          curl \
          ca-certificates \
          file \
          mesa-common-dev \
          libxkbcommon-x11-dev \
          libxkbcommon-dev \
          libx11-dev \
          libwayland-dev \
          libpcre2-dev \
          libfuse2 \
          meson \
          libxerces-c-dev \
          clang \
          llvm \
          libclang-dev

        # Install Python 3
        apt-get install -y \
          python3 \
          python3-dev \
          python3-venv \
          pyside6-tools \
          libpyside6-dev \
          shiboken6 \
          libshiboken6-dev

        # Install Qt6
        apt-get install -y \
          qt6-base-dev \
          qt6-tools-dev \
          qt6-base-dev-tools \
          qt6-tools-dev-tools \
          qt6-l10n-tools \
          libqt6svg6-dev \
          libqt6core5compat6-dev \
          qt6-declarative-dev \
          qt6-declarative-dev-tools \
          qml6-module-qtquick-controls \
          libqt6core5compat6

        # Install Cutter dependencies
        apt-get install -y \
          libgraphviz-dev \
          libkf6syntaxhighlighting-dev \
          libegl1-mesa-dev \
          libgl1-mesa-dev \
          libgles2-mesa-dev \
          libxkbcommon-dev \
          libx11-dev \
          libwayland-dev

        # Install Rizin dependencies
        apt-get install -y \
          libcapstone-dev \
          liblz4-dev \
          liblzma-dev \
          libmagic-dev \
          libzip-dev \
          libxxhash-dev \
          libssl-dev \
          libsqlite3-dev \
          libzstd-dev

    - name: Build Rizin
      shell: bash
      run: |
        cd rizin
    
        meson setup build \
          --prefix=/usr \
          --libdir=lib \
          --buildtype=release
    
        ninja -C build
    
        rm -rf "$GITHUB_WORKSPACE/rizin-appdir"
    
        DESTDIR="$GITHUB_WORKSPACE/rizin-appdir" \
          ninja -C build install

    - name: Package Rizin DEB package
      shell: bash
      run: |
        set -e

        PKGROOT="$PWD/rizin-deb"
        STAGE="$PWD/rizin-appdir"

        rm -rf "$PKGROOT" debian
        mkdir -p "$PKGROOT/DEBIAN" debian

        # Get Rizin version.
        export LD_LIBRARY_PATH="$STAGE/usr/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

        VERSION=$("$STAGE/usr/bin/rizin" -v | awk 'NR==1 {print $2}')
        ARCH=$(dpkg --print-architecture)

        echo "Rizin version: ${VERSION}"
        echo "Architecture: ${ARCH}"

        echo "RIZIN_VERSION=${VERSION}" >> "$GITHUB_ENV"

        # Install staged files.
        cp -a "$STAGE/usr" "$PKGROOT/"

        # Generate shlibs metadata.
        : > "$PKGROOT/DEBIAN/shlibs"

        find "$PKGROOT/usr/lib" -type f -name 'librz_*.so.*' -print0 |
        while IFS= read -r -d '' LIB; do
          SONAME=$(readelf -d "$LIB" |
            sed -n 's/.*SONAME.*\[\(.*\)\].*/\1/p' |
            head -n1)

          if [ -n "$SONAME" ]; then
            NAME="${SONAME%%.so.*}"
            ABI="${SONAME#*.so.}"
            echo "$NAME $ABI rizin (>= $VERSION)" \
              >> "$PKGROOT/DEBIAN/shlibs"
          fi
        done

        sort -u -o \
          "$PKGROOT/DEBIAN/shlibs" \
          "$PKGROOT/DEBIAN/shlibs"

        # Minimal control file required by dpkg-shlibdeps.
        cat > debian/control <<EOF
        Source: rizin
        Package: rizin
        Architecture: ${ARCH}
        Depends: \${shlibs:Depends}
        EOF

        # Calculate dependencies of Rizin binaries/libraries.
        ELF_FILES=$(find "$PKGROOT/usr" -type f -exec file {} + |
          awk -F: '$2 ~ /ELF/ {print $1}')

        SHLIB_DEPS=$(dpkg-shlibdeps \
          -x rizin \
          -S "$PKGROOT" \
          -l "$PKGROOT/usr/lib" \
          -O \
          $ELF_FILES |
          sed 's/^shlibs:Depends=//')

        # Create final package control.
        cat > "$PKGROOT/DEBIAN/control" <<EOF
        Package: rizin
        Version: ${VERSION}
        Architecture: ${ARCH}
        Maintainer: RizinOrg <info@rizin.re>
        Section: devel
        Priority: optional
        Depends: ${SHLIB_DEPS}
        Installed-Size: $(du -ks "$PKGROOT/usr" | cut -f1)
        Homepage: https://rizin.re
        Description: UNIX-like reverse engineering framework
         Rizin is a UNIX-like reverse engineering framework and command-line
         toolset.
        EOF

        # Generate md5sums.
        cd "$PKGROOT"

        find usr -type f -print0 |
          xargs -0 md5sum > DEBIAN/md5sums

        # Build DEB.
        cd "$GITHUB_WORKSPACE"

        dpkg-deb --build \
          "$PKGROOT" \
          "rizin_${VERSION}_${ARCH}.deb"

        apt-get install -y ./rizin_*.deb

    - name: Configure & build Cutter
      shell: bash
      run: |
        export LANG=C.UTF-8
        export LC_ALL=C.UTF-8

        # Fix ExternalProject include
        sed -i '1i include(ExternalProject)' dist/CMakeLists.txt

        cmake -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCUTTER_QT=6 \
          -DCUTTER_USE_BUNDLED_RIZIN=OFF \
          -DCUTTER_ENABLE_PYTHON=ON \
          -DCUTTER_ENABLE_PYTHON_BINDINGS=ON \
          -DCUTTER_ENABLE_GRAPHVIZ=ON \
          -DCUTTER_ENABLE_KSYNTAXHIGHLIGHTING=ON \
          -DCUTTER_ENABLE_SIGDB=ON \
          -DCUTTER_ENABLE_PACKAGING=ON \
          -DCUTTER_ENABLE_DEPENDENCY_DOWNLOADS=ON \
          -DCUTTER_PACKAGE_RZ_GHIDRA=ON \
          -DCUTTER_PACKAGE_JSDEC=ON \
          -DCUTTER_PACKAGE_RZ_LIBSWIFT=ON \
          -DCUTTER_PACKAGE_RZ_LIBYARA=ON \
          -DCUTTER_PACKAGE_RZ_SILHOUETTE=ON \
          -DCMAKE_INSTALL_PREFIX=appdir/usr \
          -DCMAKE_INSTALL_RPATH="\$ORIGIN/../lib" \
          -DCMAKE_INSTALL_RPATH_USE_LINK_PATH=ON \
          -DCPACK_DEBIAN_PACKAGE_SHLIBDEPS=ON

        cd build
        ninja
        ninja install

    - name: Build Cutter DEB package
      shell: bash
      run: |
        APP_PREFIX="$PWD/appdir/usr"
        export PATH=${APP_PREFIX}/bin:$PATH
        export LD_LIBRARY_PATH=${APP_PREFIX}/lib:${APP_PREFIX}/lib/rizin/plugins:$LD_LIBRARY_PATH
        export LANG=C.UTF-8
        export LC_ALL=C.UTF-8

        cd build

        cpack -G DEB \
          -D CPACK_DEBIAN_PACKAGE_DEPENDS="rizin (>= ${RIZIN_VERSION})" \
          -D CPACK_PACKAGE_CONTACT="RizinOrg <info@rizin.re>" \
          -D CPACK_DEBIAN_PACKAGE_HOMEPAGE="https://cutter.re" \
          -D CPACK_PACKAGE_DESCRIPTION_SUMMARY="GUI for rizin reverse engineering framework" \
          -D CPACK_DEBIAN_PACKAGE_DESCRIPTION="Cutter is a Qt based GUI for reverse engineering binaries, which makes
         use of the rizin framework. Advanced users are expected to use the
         rizin CLI tools instead, which are much more powerful."

        DEB_FILE=$(ls *.deb | head -n1)

        echo "DEB_NAME=$DEB_FILE" >> $GITHUB_ENV

    - uses: actions/upload-artifact@v4
      with:
        name: linux-arm64-artifacts
        path: |
          rizin_*.deb
          build/*.deb

    - name: Upload release asset
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v2
      with:
        files: |
          rizin_*.deb
          build/*.deb
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}